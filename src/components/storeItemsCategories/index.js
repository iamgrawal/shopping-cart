import React, { Component } from 'react';
import PropTypes from 'prop-types';

export default class StoreItemsCategories extends Component {
	static propTypes = {
		prop: PropTypes
	};

	render() {
		return <div>StoreItemsCategories</div>;
	}
}
