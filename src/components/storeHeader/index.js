import React, { Component } from 'react';
import PropTypes from 'prop-types';

export default class StoreHeader extends Component {
	static propTypes = {
		prop: PropTypes
	};

	render() {
		return <div>StoreHeader</div>;
	}
}
